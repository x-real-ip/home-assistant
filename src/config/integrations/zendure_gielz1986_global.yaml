---
input_select:
  zendure_operation_mode:
    name: Zendure Operation Mode
    icon: mdi:battery-charging-wireless
    options:
      - Standby
      - Manual
      - Smart Matching
      - Smart Discharge Only
      - Smart Charge Only
      - Dynamic Smart Matching
      - Dynamic Smart Matching (Expensive)
      - Dynamic Trading
      - Dynamic Trading + Smart Matching
      - Quick Charge
      - Quick Discharge

input_button:
  zendure_setting_set_default_settings:
    name: Zendure Set Default Settings
    icon: mdi:hand-pointer

input_boolean:
  dynamic_setting_15_minute_interval:
    name: Dynamic 15 Minutes Interval
    icon: mdi:clock-time-three-outline

  dynamic_recently_charged:
    name: Dynamic Recently Charged
    icon: mdi:battery-check

  dynamic_charging_running:
    name: Dynamic Charging Running
    icon: mdi:battery-check

  dynamic_discharging_running:
    name: Dynamic Discharging Running
    icon: mdi:battery-check

  dashboard_setting_show_dynamic:
    name: Show Dynamic On Dashboard
    icon: mdi:cash

  dashboard_setting_show_help:
    name: Show Help On Dashboard
    icon: mdi:information-outline

  dashboard_setting_show_pv:
    name: Show PV On Dashboard
    icon: mdi:solar-panel

  zendure_setting_soc_protection_disabled:
    name: Zendure SOC Protection Disabled
    icon: mdi:battery-heart-variant

  zendure_setting_pv_export_disabled:
    name: Zendure PV Export Disabled
    icon: mdi:solar-panel

input_text:
  zendure_setting_battery_order:
    name: Zendure Battery Order
    icon: mdi:flip-vertical
    max: 44
    mode: text

  zendure_setting_ip_address:
    name: Zendure IP-address
    icon: mdi:ip
    max: 50
    mode: text

  homewizard_setting_p1_ip_address:
    name: Homewizard P1 IP-address
    icon: mdi:ip
    max: 50
    mode: text

  home_energy_setting_meter_sensor:
    name: Home Energy Meter Sensor
    icon: mdi:transmission-tower
    max: 250
    mode: text

  dynamic_setting_nordpool_sensor:
    name: Dynamic Nordpool Sensor
    icon: mdi:cash
    max: 250
    mode: text

  dynamic_manual_period:
    name: Dynamic Manual Period
    icon: mdi:view-grid
    max: 250
    mode: text

  dynamic_manual_period_tomorrow:
    name: Dynamic Manual Period Tomorrow
    icon: mdi:view-grid
    max: 250
    mode: text

input_number:
  dynamic_export_correction:
    name: Dynamic Export Correction
    icon: mdi:cash
    min: 0
    max: 0.20
    step: 0.001
    mode: box
    unit_of_measurement: "€/kWh"

  zendure_setting_max_charge_power:
    name: Zendure Maximum Charge Power
    icon: mdi:format-vertical-align-top
    min: 400
    max: 12000
    step: 1
    mode: box
    unit_of_measurement: "W"

  zendure_setting_max_discharge_power:
    name: Zendure Maximum Discharge Power
    icon: mdi:format-vertical-align-bottom
    min: 400
    max: 12000
    step: 1
    mode: box
    unit_of_measurement: "W"

  zendure_setting_start_discharging_at:
    name: Zendure Start Discharging At
    icon: mdi:battery-minus-variant
    min: 80
    max: 500
    step: 1
    mode: box
    unit_of_measurement: "W"

  zendure_setting_start_charging_at:
    name: Zendure Start Charging At
    icon: mdi:battery-plus-variant
    min: -1000
    max: -80
    step: 1
    mode: box
    unit_of_measurement: "W"

  zendure_setting_standby_delay:
    name: Zendure Standby Delay
    icon: mdi:pause
    min: 5
    max: 30
    step: 1
    mode: box
    unit_of_measurement: "Min"

  zendure_setting_charge_buffer:
    name: Zendure Charge Buffer
    icon: mdi:arrow-expand-vertical
    min: 0
    max: 250
    step: 1
    mode: box
    unit_of_measurement: "W"

  zendure_setting_discharge_buffer:
    name: Zendure Discharge Buffer
    icon: mdi:arrow-expand-vertical
    min: 0
    max: 250
    step: 1
    mode: box
    unit_of_measurement: "W"

  zendure_manual_power:
    name: Zendure Manual Power
    icon: mdi:flash
    min: -12000
    max: 12000
    step: 1
    mode: box
    unit_of_measurement: "W"

  dynamic_setting_minimal_spread:
    name: Dynamic Minimal Spread
    icon: mdi:percent-box
    min: 0
    max: 100
    step: 1
    unit_of_measurement: "%"
    mode: box

  dynamic_lowest_price_x_period:
    name: Dynamic Lowest Price X Period
    icon: mdi:view-grid-plus
    min: 0
    max: 96
    step: 1
    mode: box

  dynamic_highest_price_x_period:
    name: Dynamic Highest Price X Period
    icon: mdi:view-grid-plus
    min: 0
    max: 96
    step: 1
    mode: box

  dynamic_lowest_price_x_period_tomorrow:
    name: Dynamic Lowest Price X Period Tomorrow
    icon: mdi:view-grid-plus
    min: 0
    max: 96
    step: 1
    mode: box

  dynamic_highest_price_x_period_tomorrow:
    name: Dynamic Highest Price X Period Tomorrow
    icon: mdi:view-grid-plus
    min: 0
    max: 96
    step: 1
    mode: box

  zendure_setting_minimum_allowed_state_of_charge:
    name: Zendure Minimum Allowed State Of Charge
    icon: mdi:percent-box
    min: 5
    max: 50
    step: 1
    unit_of_measurement: "%"
    mode: box

  zendure_setting_maximum_allowed_state_of_charge:
    name: Zendure Maximum Allowed State Of Charge
    icon: mdi:percent-box
    min: 70
    max: 100
    step: 1
    unit_of_measurement: "%"
    mode: box

input_datetime:
  zendure_gielz_automation_last_time_executed:
    name: Zendure Gielz Automation Last Time Executed
    icon: mdi:clock-time-eight-outline
    has_date: true
    has_time: true

sensor:
  - platform: history_stats
    name: "Home Energy Zero Import Duration Today"
    unique_id: home_energy_zero_import_duration_today
    entity_id: binary_sensor.home_energy_zero_import_active
    state: "on"
    type: time
    start: "{{ today_at() if now() < today_at('23:59') else now() }}"
    end: "{{ now() }}"

  - platform: history_stats
    name: "Zendure Relay Switches Charging Today"
    unique_id: zendure_relay_switches_charging_today
    entity_id: sensor.zendure_charging_mode
    state: "Charging"
    type: count
    start: "{{ today_at() if now() < today_at('23:59') else now() }}"
    end: "{{ now() }}"

  - platform: history_stats
    name: "Zendure Relay Switches Discharging Today"
    unique_id: zendure_relay_switches_discharging_today
    entity_id: sensor.zendure_charging_mode
    state: "Discharging"
    type: count
    start: "{{ today_at() if now() < today_at('23:59') else now() }}"
    end: "{{ now() }}"

  - platform: statistics
    name: "Zendure Import Efficiency 24h"
    unique_id: zendure_import_efficiency_24h
    entity_id: sensor.zendure_import_efficiency
    state_characteristic: mean
    sampling_size: 86400
    max_age:
      hours: 24

  - platform: statistics
    name: "Zendure Export Efficiency 24h"
    unique_id: zendure_export_efficiency_24h
    entity_id: sensor.zendure_export_efficiency
    state_characteristic: mean
    sampling_size: 86400
    max_age:
      hours: 24

  - platform: integration
    name: "Zendure PV Import"
    unique_id: zendure_pv_import
    source: sensor.zendure_power_pv_total
    unit_prefix: k
    round: 2
    method: trapezoidal

  - platform: integration
    name: "Zendure Energy Import"
    unique_id: zendure_energy_import
    source: sensor.zendure_power_import
    unit_prefix: k
    round: 2
    method: trapezoidal

  - platform: integration
    name: "Zendure Energy Export"
    unique_id: zendure_energy_export
    source: sensor.zendure_power_export
    unit_prefix: k
    round: 2
    method: trapezoidal

  - platform: rest
    name: "GitHub Zendure Gielz1986"
    unique_id: github_zendure_gielz1986
    resource: "https://api.github.com/repos/Gielz1986/Zendure-HA-zenSDK/releases/latest"
    headers:
      User-Agent: "HomeAssistant"
    scan_interval: 3600
    json_attributes:
      - tag_name
      - name
      - published_at
      - body
    value_template: "{{ value_json.name }}"
    icon: mdi:github

utility_meter:
  zendure_pv_import_daily:
    source: sensor.zendure_pv_import
    unique_id: utility_meter.zendure_pv_import_daily
    name: Zendure PV Import Daily
    cycle: daily

template:
  - binary_sensor:
      - name: "Home Energy Zero Import Active"
        unique_id: home_energy_zero_import_active
        device_class: power
        state: "{{ states('sensor.home_energy_meter_power') | float(0) < 1 }}"
        icon: >
          {% if states('sensor.home_energy_meter_power') | float(0) < 1 %}
            mdi:leaf
          {% else %}
            mdi:flash
          {% endif %}

  - trigger:
      - platform: state
        entity_id: sensor.zendure_total_state_of_charge
        to: "100"

    sensor:
      - name: "Zendure Last Calibration"
        unique_id: zendure_last_calibration
        device_class: timestamp
        icon: mdi:sync
        state: >
          {% if trigger.platform == 'state' %}
            {{ now().isoformat() }}
          {% else %}
            {{ this.state }}
          {% endif %}

  - sensor:
      - name: "Zendure Configuration Version"
        unique_id: zendure_configuration_version
        icon: mdi:one-up
        state: "v20260716"

      - name: "Zendure Last Calibration (Days)"
        unique_id: zendure_last_calibration_days
        unit_of_measurement: "d"
        state: >
          {% set last = states('sensor.zendure_last_calibration') %}
          {% if last in ['unknown','unavailable',''] %}
            0
          {% else %}
          {{ (now().date() - as_datetime(last).date()).days }}
          {% endif %}
        icon: >
          {% if this.state | int(0) < 29 %}
            mdi:thumb-up
          {% else %}
            mdi:thumb-down
          {% endif %}

      - name: "Github Zendure Gielz1986 Releasecheck"
        unique_id: github_zendure_gielz1986_releasecheck
        state: >
          {% set config_date = states('sensor.zendure_configuration_version')
            | regex_findall('v(\d{8})') | first %}
          {% set github_raw = state_attr('sensor.github_zendure_gielz1986', 'tag_name') %}
          {% if github_raw in ['unknown', 'unavailable', None] %}
          {% set github_date = None %}
          {% else %}
          {% set github_date = github_raw | regex_replace('v','') %}
          {% endif %}
          {% if config_date and github_date %}
          {% if config_date > github_date %}
            Pre-release Active
          {% elif config_date < github_date %}
            New Release On GitHub
          {% else %}
            Up-to-date
          {% endif %}
          {% else %}
            Unknown
          {% endif %}
        icon: >
          {% if this.state == 'Up-to-date' %}
           mdi:emoticon-happy-outline
          {% elif this.state == 'New Release On GitHub' %}
           mdi:emoticon-sad-outline
          {% elif this.state == 'Pre-release Active' %}
           mdi:test-tube
          {% else %}
           mdi:emoticon-sad-outline
          {% endif %}

      - name: "Home Energy Meter Target"
        unique_id: home_energy_meter_target
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power
        state: >
          {{ (states('sensor.home_energy_meter_power') | float(0) + (states('sensor.zendure_power') | float(0) * -1)) | round(0) }}

      - name: "Zendure Relay Switches Today"
        unique_id: zendure_relay_switches_today
        icon: mdi:swap-vertical-bold
        state_class: total_increasing
        state: >
          {% set ontladen = states('sensor.zendure_relay_switches_discharging_today') | int(0) %}
          {% set opladen = states('sensor.zendure_relay_switches_charging_today') | int(0) %}
          {% set totaal = ontladen + opladen - 1 %}
          {{ totaal if totaal > 0 else 0 }}

      - name: "Home Energy Meter Power"
        unique_id: home_energy_meter_power
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power
        availability: >
          {% set afwijkend = states('input_text.home_energy_setting_meter_sensor') %}
          {% set afwijkend_valid = afwijkend | lower | regex_match('^sensor\\..+') and states(afwijkend) not in ['unknown', 'unavailable', 'none'] %}
          {% set homewizard_valid = states('sensor.homewizard_p1_power') not in ['unknown', 'unavailable', 'none'] %}
            {{ afwijkend_valid or homewizard_valid }}
        state: >
          {% set afwijkend = states('input_text.home_energy_setting_meter_sensor') %}
          {% if afwijkend | lower | regex_match('^sensor\\..+') and states(afwijkend) not in ['unknown', 'unavailable', 'none'] %}
          {{ states(afwijkend) | float(0) }}
          {% else %}
          {{ states('sensor.homewizard_p1_power') | float(0) }}
          {% endif %}

      - name: "Dynamic Nordpool"
        unique_id: dynamic_nordpool
        icon: mdi:cash
        unit_of_measurement: "€/kWh"
        state: >-
          {% set ingevuld = states('input_text.dynamic_setting_nordpool_sensor') %}
          {% if ingevuld | lower | regex_match('^sensor\\..+') and states(ingevuld) not in ['unknown', 'unavailable', 'none'] %}
            {% set nordpool = ingevuld %}
          {% else %}
            {% set nordpool = none %}
          {% endif %}

          {% if nordpool is not none %}
            {% set use_15_min = is_state('input_boolean.dynamic_setting_15_minute_interval', 'on') %}
            {% if use_15_min %}
              {% set data = state_attr(nordpool, 'raw_today') or [] %}
              {% set nowts = as_timestamp(now()) %}
              {% set current = namespace(val=None) %}
              {% for p in data %}
                {% set s = as_timestamp(as_datetime(p.start)) %}
                {% set e = as_timestamp(as_datetime(p.end)) %}
                {% if nowts >= s and nowts < e %}
                  {% set current.val = (p.value) | round(5) %}
                {% endif %}
              {% endfor %}
              {{ current.val if current.val is not none else 'unknown' }}
            {% else %}
              {% set raw = state_attr(nordpool, 'raw_today') %}
              {% set nu = now() %}
              {% if raw %}
                {% set current_hour_values = raw
                  | selectattr('start', '>=', nu.replace(minute=0, second=0, microsecond=0))
                  | selectattr('start', '<', nu.replace(minute=0, second=0, microsecond=0) + timedelta(hours=1))
                  | map(attribute='value')
                  | list %}
                {% if current_hour_values %}
                  {{ (current_hour_values | sum / current_hour_values | count) | round(5) }}
                {% else %}
                  {{ states(nordpool) }}
                {% endif %}
              {% else %}
                {{ states(nordpool) }}
              {% endif %}
            {% endif %}
          {% else %}
            0
          {% endif %}
        attributes:
          raw_today: >-
            {% set ingevuld = states('input_text.dynamic_setting_nordpool_sensor') %}
            {% if ingevuld | lower | regex_match('^sensor\\..+') and states(ingevuld) not in ['unknown', 'unavailable', 'none'] %}
              {% set nordpool = ingevuld %}
            {% else %}
              {% set nordpool = none %}
            {% endif %}

            {% if nordpool is not none %}
              {% set use_15_min = is_state('input_boolean.dynamic_setting_15_minute_interval', 'on') %}
              {% set raw = state_attr(nordpool, 'raw_today') %}
              {% if raw %}
                [
                {% if use_15_min %}
                  {% for item in raw %}
                    {
                      "start": "{{ item.start.isoformat() }}",
                      "end": "{{ item.end.isoformat() }}",
                      "value": {{ item.value }}
                    }{% if not loop.last %},{% endif %}
                  {% endfor %}
                {% else %}
                  {% set hourly = raw | groupby('start.hour') %}
                  {% for hour_group in hourly %}
                    {% set values = hour_group.list | map(attribute='value') | list %}
                    {% set hour_start = hour_group.list[0].start.replace(minute=0, second=0, microsecond=0) %}
                    {% set hour_end = hour_start + timedelta(hours=1) %}
                    {
                      "start": "{{ hour_start.isoformat() }}",
                      "end": "{{ hour_end.isoformat() }}",
                      "value": {{ (values | sum / values | count) | round(5) }}
                    }{% if not loop.last %},{% endif %}
                  {% endfor %}
                {% endif %}
                ]
              {% else %}
                []
              {% endif %}
            {% else %}
              []
            {% endif %}
          raw_tomorrow: >-
            {% set ingevuld = states('input_text.dynamic_setting_nordpool_sensor') %}
            {% if ingevuld | lower | regex_match('^sensor\\..+') and states(ingevuld) not in ['unknown', 'unavailable', 'none'] %}
              {% set nordpool = ingevuld %}
            {% else %}
              {% set nordpool = none %}
            {% endif %}

            {% if nordpool is not none %}
              {% set use_15_min = is_state('input_boolean.dynamic_setting_15_minute_interval', 'on') %}
              {% set raw = state_attr(nordpool, 'raw_tomorrow') %}
              {% if raw %}
                [
                {% if use_15_min %}
                  {% for item in raw %}
                    {
                      "start": "{{ item.start.isoformat() }}",
                      "end": "{{ item.end.isoformat() }}",
                      "value": {{ item.value }}
                    }{% if not loop.last %},{% endif %}
                  {% endfor %}
                {% else %}
                  {% set hourly = raw | groupby('start.hour') %}
                  {% for hour_group in hourly %}
                    {% set values = hour_group.list | map(attribute='value') | list %}
                    {% set hour_start = hour_group.list[0].start.replace(minute=0, second=0, microsecond=0) %}
                    {% set hour_end = hour_start + timedelta(hours=1) %}
                    {
                      "start": "{{ hour_start.isoformat() }}",
                      "end": "{{ hour_end.isoformat() }}",
                      "value": {{ (values | sum / values | count) | round(5) }}
                    }{% if not loop.last %},{% endif %}
                  {% endfor %}
                {% endif %}
                ]
              {% else %}
                []
              {% endif %}
            {% else %}
              []
            {% endif %}
          max_tomorrow: >-
            {% set data = state_attr('sensor.dynamic_nordpool','raw_tomorrow') %}
            {% if data %}
            {{ data | map(attribute='value') | max | round(5) }}
            {% else %}
              0
            {% endif %}
          min_tomorrow: >-
            {% set data = state_attr('sensor.dynamic_nordpool','raw_tomorrow') %}
            {% if data %}
            {{ data | map(attribute='value') | min | round(5) }}
            {% else %}
              0
            {% endif %}
          max_today: >-
            {% set data = state_attr('sensor.dynamic_nordpool','raw_today') %}
            {% if data %}
            {{ data | map(attribute='value') | max | round(5) }}
            {% else %}
              0
            {% endif %}
          min_today: >-
            {% set data = state_attr('sensor.dynamic_nordpool','raw_today') %}
            {% if data %}
            {{ data | map(attribute='value') | min | round(5) }}
            {% else %}
              0
            {% endif %}

      - name: "Dynamic Spread Indication"
        unique_id: dynamic_spread_indication
        icon: mdi:percent-box
        unit_of_measurement: "%"
        state: >
          {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_today') or [] %}
          {% set correction = states('input_number.dynamic_export_correction') | float(0) %}
          {% set goedkoop = uren | selectattr('goedkoop','equalto','Yes') | list %}
          {% set duur = uren | selectattr('duur','equalto','Yes') | list %}
          {% if goedkoop | count > 0 and duur | count > 0 %}
            {% set avg_goedkoop = goedkoop | map(attribute='value') | sum / goedkoop | count %}
            {% set avg_duur = (duur | map(attribute='value') | sum / duur | count) - correction %}
            {{ ((avg_duur - avg_goedkoop) / (avg_goedkoop | abs) * 100) | round(1) }}
          {% else %}
            0
          {% endif %}
        attributes:
          manual_periods: >
            {% if states('input_text.dynamic_manual_period') | length > 0 %}
              n.v.t.
            {% else %}
              {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_today') or [] %}
              {% set ns = namespace(result=[]) %}
              {% for uur in uren %}
                {% if uur.goedkoop == 'Yes' %}
                  {% set ns.result = ns.result + ['G' ~ (uur.start | as_datetime).strftime('%H:%M')] %}
                {% elif uur.duur == 'Yes' %}
                  {% set ns.result = ns.result + ['D' ~ (uur.start | as_datetime).strftime('%H:%M')] %}
                {% endif %}
              {% endfor %}
              {{ ns.result | join(';') }}
            {% endif %}
          lowest_price_periods: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_today') or [] %}
            {% set goedkoop = uren | selectattr('goedkoop','equalto','Yes') | sort(attribute='start') | list %}
            [
            {% for uur in goedkoop %}
              "{{ (uur.start | as_datetime).strftime('%H:%M') }} - €{{ '%.4f' | format(uur.value) }}"
              {% if not loop.last %}, {% endif %}
            {% endfor %}
            ]
          highest_price_periods: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_today') or [] %}
            {% set correction = states('input_number.dynamic_export_correction') | float(0) %}
            {% set duur = uren | selectattr('duur','equalto','Yes') | sort(attribute='start') | list %}
            [
            {% for uur in duur %}
              "{{ (uur.start | as_datetime).strftime('%H:%M') }} - €{{ '%.4f' | format(uur.value - correction) }}"
              {% if not loop.last %}, {% endif %}
            {% endfor %}
            ]
          average_lowest_price: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_today') or [] %}
            {% set goedkoop = uren | selectattr('goedkoop','equalto','Yes') | list %}
            {% if goedkoop | count > 0 %}
              €{{ '%.4f' % (goedkoop | map(attribute='value') | sum / goedkoop | count) }}
            {% else %}
              n.v.t.
            {% endif %}
          average_highest_price: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_today') or [] %}
            {% set correction = states('input_number.dynamic_export_correction') | float(0) %}
            {% set duur = uren | selectattr('duur','equalto','Yes') | list %}
            {% if duur | count > 0 %}
              €{{ '%.4f' % ((duur | map(attribute='value') | sum / duur | count) - correction) }}
            {% else %}
              n.v.t.
            {% endif %}
          difference_in_value: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_today') or [] %}
            {% set correction = states('input_number.dynamic_export_correction') | float(0) %}
            {% set goedkoop = uren | selectattr('goedkoop','equalto','Yes') | list %}
            {% set duur = uren | selectattr('duur','equalto','Yes') | list %}
            {% if goedkoop | count > 0 and duur | count > 0 %}
              {% set avg_goedkoop = goedkoop | map(attribute='value') | sum / goedkoop | count %}
              {% set avg_duur = (duur | map(attribute='value') | sum / duur | count) - correction %}
              €{{ '%.4f' % (avg_duur - avg_goedkoop) }}
            {% else %}
              n.v.t.
            {% endif %}

      - name: "Dynamic Spread Indication DSC"
        unique_id: dynamic_spread_indication_dsc
        icon: mdi:percent-box
        unit_of_measurement: "%"
        state: >
          {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_today') or [] %}
          {% set duurste = state_attr('sensor.dynamic_lowest_price_period', 'duurste_na_eerste_goedkope') or [] %}

          {% set goedkope_uren = uren | selectattr('goedkoop','equalto','Yes') | list %}
          {% set dure_uren = duurste | list %}

          {% if goedkope_uren | count > 0 and dure_uren | count > 0 %}
            {% set avg_goedkoop = goedkope_uren | map(attribute='value') | sum / goedkope_uren | count %}
            {% set avg_duur = dure_uren | map(attribute='value') | sum / dure_uren | count %}
            {{ ((avg_duur - avg_goedkoop) / (avg_goedkoop | abs) * 100) | round(1) }}
          {% else %}
            0
          {% endif %}
        attributes:
          manual_periods: >
            {% if states('input_text.dynamic_manual_period') | length > 0 %}
              n.v.t.
            {% else %}
              {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_today') or [] %}
              {% set ns = namespace(result=[]) %}
              {% for uur in uren %}
                {% if uur.goedkoop == 'Yes' %}
                  {% set ns.result = ns.result + ['G' ~ (uur.start | as_datetime).strftime('%H:%M')] %}
                {% elif uur.duur == 'Yes' %}
                  {% set ns.result = ns.result + ['D' ~ (uur.start | as_datetime).strftime('%H:%M')] %}
                {% endif %}
              {% endfor %}
              {{ ns.result | join(';') }}
            {% endif %}
          lowest_price_periods: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_today') or [] %}
            {% set goedkoop = uren | selectattr('goedkoop','equalto','Yes') | sort(attribute='start') | list %}
            [
            {% for uur in goedkoop %}
              "{{ (uur.start | as_datetime).strftime('%H:%M') }} - €{{ '%.4f' | format(uur.value) }}"
              {% if not loop.last %}, {% endif %}
            {% endfor %}
            ]
          highest_price_periods: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'duurste_na_eerste_goedkope') or [] %}
            {% set duur = uren | sort(attribute='value', reverse=true) | list %}
            [
            {% for uur in duur %}
              "{{ (uur.start | as_datetime).strftime('%H:%M') }} - €{{ '%.4f' | format(uur.value) }}"
              {% if not loop.last %}, {% endif %}
            {% endfor %}
            ]
          average_lowest_price: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_today') or [] %}
            {% set goedkoop = uren | selectattr('goedkoop','equalto','Yes') | list %}
            {% if goedkoop | count > 0 %}
              €{{ '%.4f' % (goedkoop | map(attribute='value') | sum / goedkoop | count) }}
            {% else %}
              n.v.t.
            {% endif %}
          average_highest_price: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'duurste_na_eerste_goedkope') or [] %}
            {% set duur = uren | selectattr('duur','equalto','Yes') | list %}
            {% if duur | count > 0 %}
              €{{ '%.4f' % (duur | map(attribute='value') | sum / duur | count) }}
            {% else %}
              n.v.t.
            {% endif %}
          difference_in_value: >
            {% set g = state_attr('sensor.dynamic_spread_indication_dsc','average_lowest_price') %}
            {% set d = state_attr('sensor.dynamic_spread_indication_dsc','average_highest_price') %}
            {% if g not in ['n.v.t.', None] and d not in ['n.v.t.', None] %}
              €{{ '%.4f' % (d[1:] | float - g[1:] | float) }}
            {% else %}
              n.v.t.
            {% endif %}

      - name: "Dynamic Spread Indication Tomorrow"
        unique_id: dynamic_spread_indication_tomorrow
        icon: mdi:percent-box
        unit_of_measurement: "%"
        state: >
          {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_tomorrow') or [] %}
          {% set correction = states('input_number.dynamic_export_correction') | float(0) %}
          {% set goedkoop = uren | selectattr('goedkoop','equalto','Yes') | list %}
          {% set duur = uren | selectattr('duur','equalto','Yes') | list %}
          {% if goedkoop | count > 0 and duur | count > 0 %}
            {% set avg_goedkoop = goedkoop | map(attribute='value') | sum / goedkoop | count %}
            {% set avg_duur = (duur | map(attribute='value') | sum / duur | count) - correction %}
            {{ ((avg_duur - avg_goedkoop) / (avg_goedkoop | abs) * 100) | round(1) }}
          {% else %}
            0
          {% endif %}
        attributes:
          manual_periods: >
            {% if states('input_text.dynamic_manual_period_tomorrow') | length > 0 %}
              n.v.t.
            {% else %}
              {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_tomorrow') or [] %}
              {% set ns = namespace(result=[]) %}
              {% for uur in uren %}
                {% if uur.goedkoop == 'Yes' %}
                  {% set ns.result = ns.result + ['G' ~ (uur.start | as_datetime).strftime('%H:%M')] %}
                {% elif uur.duur == 'Yes' %}
                  {% set ns.result = ns.result + ['D' ~ (uur.start | as_datetime).strftime('%H:%M')] %}
                {% endif %}
              {% endfor %}
              {{ ns.result | join(';') }}
            {% endif %}
          lowest_price_periods: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_tomorrow') or [] %}
            {% set goedkoop = uren | selectattr('goedkoop','equalto','Yes') | sort(attribute='start') | list %}
            [
            {% for uur in goedkoop %}
              "{{ (uur.start | as_datetime).strftime('%H:%M') }} - €{{ '%.4f' | format(uur.value) }}"
              {% if not loop.last %}, {% endif %}
            {% endfor %}
            ]
          highest_price_periods: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_tomorrow') or [] %}
            {% set correction = states('input_number.dynamic_export_correction') | float(0) %}
            {% set duur = uren | selectattr('duur','equalto','Yes') | sort(attribute='start') | list %}
            [
            {% for uur in duur %}
              "{{ (uur.start | as_datetime).strftime('%H:%M') }} - €{{ '%.4f' | format(uur.value - correction) }}"
              {% if not loop.last %}, {% endif %}
            {% endfor %}
            ]
          average_lowest_price: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_tomorrow') or [] %}
            {% set goedkoop = uren | selectattr('goedkoop','equalto','Yes') | list %}
            {% if goedkoop | count > 0 %}
              €{{ '%.4f' % (goedkoop | map(attribute='value') | sum / goedkoop | count) }}
            {% else %}
              n.v.t.
            {% endif %}
          average_highest_price: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_tomorrow') or [] %}
            {% set correction = states('input_number.dynamic_export_correction') | float(0) %}
            {% set duur = uren | selectattr('duur','equalto','Yes') | list %}
            {% if duur | count > 0 %}
              €{{ '%.4f' % ((duur | map(attribute='value') | sum / duur | count) - correction) }}
            {% else %}
              n.v.t.
            {% endif %}
          difference_in_value: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_tomorrow') or [] %}
            {% set correction = states('input_number.dynamic_export_correction') | float(0) %}
            {% set goedkoop = uren | selectattr('goedkoop','equalto','Yes') | list %}
            {% set duur = uren | selectattr('duur','equalto','Yes') | list %}
            {% if goedkoop | count > 0 and duur | count > 0 %}
              {% set avg_goedkoop = goedkoop | map(attribute='value') | sum / goedkoop | count %}
              {% set avg_duur = (duur | map(attribute='value') | sum / duur | count) - correction %}
              €{{ '%.4f' % (avg_duur - avg_goedkoop) }}
            {% else %}
              n.v.t.
            {% endif %}

      - name: "Dynamic Spread Indication DSC Tomorrow"
        unique_id: dynamic_spread_indication_dsc_tomorrow
        icon: mdi:percent-box
        unit_of_measurement: "%"
        state: >
          {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_tomorrow') or [] %}
          {% set duurste = state_attr('sensor.dynamic_lowest_price_period', 'duurste_na_eerste_goedkope_tomorrow') or [] %}
          {% set goedkope_uren = uren | selectattr('goedkoop','equalto','Yes') | list %}
          {% set dure_uren = duurste | list %}
          {% if goedkope_uren | count > 0 and dure_uren | count > 0 %}
            {% set avg_goedkoop = goedkope_uren | map(attribute='value') | sum / goedkope_uren | count %}
            {% set avg_duur = dure_uren | map(attribute='value') | sum / dure_uren | count %}
            {{ ((avg_duur - avg_goedkoop) / (avg_goedkoop | abs) * 100) | round(1) }}
          {% else %}
            0
          {% endif %}
        attributes:
          manual_periods: >
            {% if states('input_text.dynamic_manual_period_tomorrow') | length > 0 %}
              n.v.t.
            {% else %}
              {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_tomorrow') or [] %}
              {% set ns = namespace(result=[]) %}
              {% for uur in uren %}
                {% if uur.goedkoop == 'Yes' %}
                  {% set ns.result = ns.result + ['G' ~ (uur.start | as_datetime).strftime('%H:%M')] %}
                {% elif uur.duur == 'Yes' %}
                  {% set ns.result = ns.result + ['D' ~ (uur.start | as_datetime).strftime('%H:%M')] %}
                {% endif %}
              {% endfor %}
              {{ ns.result | join(';') }}
            {% endif %}
          lowest_price_periods: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_tomorrow') or [] %}
            {% set goedkoop = uren | selectattr('goedkoop','equalto','Yes') | sort(attribute='start') | list %}
            [
            {% for uur in goedkoop %}
              "{{ (uur.start | as_datetime).strftime('%H:%M') }} - €{{ '%.4f' | format(uur.value) }}"
              {% if not loop.last %}, {% endif %}
            {% endfor %}
            ]
          highest_price_periods: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'duurste_na_eerste_goedkope_tomorrow') or [] %}
            {% set duur = uren | sort(attribute='value', reverse=true) | list %}
            [
            {% for uur in duur %}
              "{{ (uur.start | as_datetime).strftime('%H:%M') }} - €{{ '%.4f' | format(uur.value) }}"
              {% if not loop.last %}, {% endif %}
            {% endfor %}
            ]
          average_lowest_price: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'raw_tomorrow') or [] %}
            {% set goedkoop = uren | selectattr('goedkoop','equalto','Yes') | list %}
            {% if goedkoop | count > 0 %}
              €{{ '%.4f' % (goedkoop | map(attribute='value') | sum / goedkoop | count) }}
            {% else %}
              n.v.t.
            {% endif %}
          average_highest_price: >
            {% set uren = state_attr('sensor.dynamic_lowest_price_period', 'duurste_na_eerste_goedkope_tomorrow') or [] %}
            {% set duur = uren | selectattr('duur','equalto','Yes') | list %}
            {% if duur | count > 0 %}
              €{{ '%.4f' % (duur | map(attribute='value') | sum / duur | count) }}
            {% else %}
              n.v.t.
            {% endif %}
          difference_in_value: >
            {% set g = state_attr('sensor.dynamic_spread_indication_dsc_tomorrow','average_lowest_price') %}
            {% set d = state_attr('sensor.dynamic_spread_indication_dsc_tomorrow','average_highest_price') %}
            {% if g not in ['n.v.t.', None] and d not in ['n.v.t.', None] %}
              €{{ '%.4f' % (d[1:] | float - g[1:] | float) }}
            {% else %}
              n.v.t.
            {% endif %}

      - name: "Dynamic Highest Price Period"
        unique_id: dynamic_highest_price_period
        icon: mdi:progress-clock
        state: >
          {% set prijzen = state_attr('sensor.dynamic_nordpool', 'raw_today') %}
          {% set handmatige = states('input_text.dynamic_manual_period') %}
          {% set aantal_duur = states('input_number.dynamic_highest_price_x_period') | int %}
          {% set kwartier = is_state('input_boolean.dynamic_setting_15_minute_interval', 'on') %}
          {% set default_delta = timedelta(minutes=15) if kwartier else timedelta(hours=1) %}
          {% if prijzen is iterable %}
            {% set ns = namespace(dure_starttijden=[]) %}
            {% if handmatige | length > 0 %}
              {% for item in handmatige.split(';') %}
                {% if item[0] == 'D' %}
                  {% set tijden = item[1:] %}
                  {% if '-' in tijden %}
                    {% set start_str = tijden.split('-')[0] %}
                    {% set eind_str = tijden.split('-')[1] %}
                    {% set range_block = True %}
                  {% else %}
                    {% set start_str = tijden %}
                    {% set range_block = False %}
                  {% endif %}
                  {% for uur in prijzen %}
                    {% set dt_start = uur.start | as_datetime %}
                    {% set dt_end = uur.end | as_datetime %}
                    {% set dag = dt_start.strftime('%Y-%m-%d') %}
                    {% set tz = dt_start.tzinfo %}
                    {% set start_dt = strptime(dag ~ ' ' ~ start_str, '%Y-%m-%d %H:%M').replace(tzinfo=tz) %}
                    {% if range_block %}
                      {% set eind_dt = strptime(dag ~ ' ' ~ eind_str, '%Y-%m-%d %H:%M').replace(tzinfo=tz) %}
                      {% if eind_dt <= start_dt %}
                        {% set eind_dt = eind_dt + timedelta(days=1) %}
                      {% endif %}
                      {% set eind_dt = eind_dt + timedelta(seconds=1) %}
                    {% else %}
                      {% set eind_dt = start_dt + default_delta %}
                    {% endif %}
                    {% if dt_start < eind_dt and dt_end > start_dt %}
                      {% set ns.dure_starttijden = ns.dure_starttijden + [uur.start] %}
                    {% endif %}
                  {% endfor %}
                {% endif %}
              {% endfor %}
            {% else %}
              {% set prijzen_sorted = prijzen | sort(attribute='value') %}
              {% if aantal_duur > 0 %}
                {% set ns.dure_starttijden = prijzen_sorted[-aantal_duur:] | map(attribute='start') | list %}
              {% else %}
                {% set ns.dure_starttijden = [] %}
              {% endif %}
            {% endif %}
            {% set nu = now().astimezone() %}
            {% set actief = prijzen
              | selectattr('start', 'in', ns.dure_starttijden)
              | selectattr('start', 'le', nu.isoformat())
              | selectattr('end', 'gt', nu.isoformat())
              | list %}
            {{ 'Yes' if actief | length > 0 else 'No' }}
          {% else %}
            Onbekend
          {% endif %}
        attributes:
          raw_today: >
            {% set prijzen = state_attr('sensor.dynamic_lowest_price_period', 'raw_today') %}
            {% set handmatige = states('input_text.dynamic_manual_period') %}
            {% set aantal_goedkoop = states('input_number.dynamic_lowest_price_x_period') | int %}
            {% set aantal_duur = states('input_number.dynamic_highest_price_x_period') | int %}
            {% if prijzen is iterable %}
              {% set ns = namespace(goede_starttijden=[], dure_starttijden=[]) %}
              {% if handmatige | length > 0 %}
                {% for item in handmatige.split(';') %}
                  {% set type = item[0] %}
                  {% set tijden = item[1:] %}
                  {% for uur in prijzen %}
                    {% if uur.start[11:16] == tijden %}
                      {% if type == 'G' %}
                        {% set ns.goede_starttijden = ns.goede_starttijden + [uur.start] %}
                      {% elif type == 'D' %}
                        {% set ns.dure_starttijden = ns.dure_starttijden + [uur.start] %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                {% endfor %}
              {% else %}
                {% set prijzen_sorted = prijzen | sort(attribute='value') %}
                {% set ns.goede_starttijden = prijzen_sorted[:aantal_goedkoop] | map(attribute='start') | list %}
                {% if aantal_duur > 0 %}
                  {% set ns.dure_starttijden = prijzen_sorted[-aantal_duur:] | map(attribute='start') | list %}
                {% else %}
                  {% set ns.dure_starttijden = [] %}
                {% endif %}
              {% endif %}
              [
              {% for uur in prijzen %}
                {
                  "start": "{{ uur.start }}",
                  "end": "{{ uur.end }}",
                  "value": {{ uur.value }},
                  "goedkoop": "{{ 'Yes' if uur.start in ns.goede_starttijden else 'No' }}",
                  "duur": "{{ 'Yes' if uur.start in ns.dure_starttijden else 'No' }}"
                }{% if not loop.last %},{% endif %}
              {% endfor %}
              ]
            {% else %}
              []
            {% endif %}

      - name: "Dynamic Lowest Price Period"
        unique_id: dynamic_lowest_price_period
        icon: mdi:progress-clock
        state: >
          {% set prijzen = state_attr('sensor.dynamic_nordpool', 'raw_today') %}
          {% set handmatige = states('input_text.dynamic_manual_period') %}
          {% set aantal_goedkoop = states('input_number.dynamic_lowest_price_x_period') | int %}
          {% set kwartier = is_state('input_boolean.dynamic_setting_15_minute_interval', 'on') %}
          {% set default_delta = timedelta(minutes=15) if kwartier else timedelta(hours=1) %}
          {% if prijzen is iterable %}
            {% set ns = namespace(goedkope_starttijden=[]) %}
            {% if handmatige | length > 0 %}
              {% for item in handmatige.split(';') %}
                {% if item[0] == 'G' %}
                  {% set tijden = item[1:] %}
                  {% if '-' in tijden %}
                    {% set start_str = tijden.split('-')[0] %}
                    {% set eind_str = tijden.split('-')[1] %}
                    {% set range_block = True %}
                  {% else %}
                    {% set start_str = tijden %}
                    {% set range_block = False %}
                  {% endif %}
                  {% for uur in prijzen %}
                    {% set dt_start = uur.start | as_datetime %}
                    {% set dt_end = uur.end | as_datetime %}
                    {% set dag = dt_start.strftime('%Y-%m-%d') %}
                    {% set tz = dt_start.tzinfo %}
                    {% set start_dt = strptime(dag ~ ' ' ~ start_str, '%Y-%m-%d %H:%M').replace(tzinfo=tz) %}
                    {% if range_block %}
                      {% set eind_dt = strptime(dag ~ ' ' ~ eind_str, '%Y-%m-%d %H:%M').replace(tzinfo=tz) %}
                      {% if eind_dt <= start_dt %}
                        {% set eind_dt = eind_dt + timedelta(days=1) %}
                      {% endif %}
                      {% set eind_dt = eind_dt + timedelta(seconds=1) %}
                    {% else %}
                      {% set eind_dt = start_dt + default_delta %}
                    {% endif %}
                    {% if dt_start < eind_dt and dt_end > start_dt %}
                      {% set ns.goedkope_starttijden = ns.goedkope_starttijden + [uur.start] %}
                    {% endif %}
                  {% endfor %}
                {% endif %}
              {% endfor %}
            {% else %}
              {% set prijzen_sorted = prijzen | sort(attribute='value') %}
              {% set ns.goedkope_starttijden = prijzen_sorted[:aantal_goedkoop] | map(attribute='start') | list %}
            {% endif %}
            {% set nu = now().astimezone() %}
            {% set in_goedkoop_blok = prijzen
              | selectattr('start', 'in', ns.goedkope_starttijden)
              | selectattr('start', 'le', nu.isoformat())
              | selectattr('end', 'gt', nu.isoformat())
              | list %}
            {{ 'Yes' if in_goedkoop_blok | length > 0 else 'No' }}
          {% else %}
            Onbekend
          {% endif %}
        attributes:
          raw_today: >
            {% set prijzen = state_attr('sensor.dynamic_nordpool', 'raw_today') %}
            {% set handmatige = states('input_text.dynamic_manual_period') %}
            {% set aantal_goedkoop = states('input_number.dynamic_lowest_price_x_period') | int %}
            {% set aantal_duur = states('input_number.dynamic_highest_price_x_period') | int %}
            {% set kwartier = is_state('input_boolean.dynamic_setting_15_minute_interval', 'on') %}
            {% set default_delta = timedelta(minutes=15) if kwartier else timedelta(hours=1) %}
            {% if prijzen is iterable %}
              {% set ns = namespace(goedkope_starttijden=[], dure_starttijden=[]) %}
              {% if handmatige | length > 0 %}
                {% for item in handmatige.split(';') %}
                  {% set type = item[0] %}
                  {% set tijden = item[1:] %}
                  {% if '-' in tijden %}
                    {% set start_str = tijden.split('-')[0] %}
                    {% set eind_str = tijden.split('-')[1] %}
                    {% set range_block = True %}
                  {% else %}
                    {% set start_str = tijden %}
                    {% set range_block = False %}
                  {% endif %}
                  {% for uur in prijzen %}
                    {% set dt_start = uur.start | as_datetime %}
                    {% set dt_end = uur.end | as_datetime %}
                    {% set dag = dt_start.strftime('%Y-%m-%d') %}
                    {% set tz = dt_start.tzinfo %}
                    {% set start_dt = strptime(dag ~ ' ' ~ start_str, '%Y-%m-%d %H:%M').replace(tzinfo=tz) %}
                    {% if range_block %}
                      {% set eind_dt = strptime(dag ~ ' ' ~ eind_str, '%Y-%m-%d %H:%M').replace(tzinfo=tz) %}
                      {% if eind_dt <= start_dt %}
                        {% set eind_dt = eind_dt + timedelta(days=1) %}
                      {% endif %}
                      {% set eind_dt = eind_dt + timedelta(seconds=1) %}
                    {% else %}
                      {% set eind_dt = start_dt + default_delta %}
                    {% endif %}
                    {% if dt_start < eind_dt and dt_end > start_dt %}
                      {% if type == 'G' %}
                        {% set ns.goedkope_starttijden = ns.goedkope_starttijden + [uur.start] %}
                      {% elif type == 'D' %}
                        {% set ns.dure_starttijden = ns.dure_starttijden + [uur.start] %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                {% endfor %}
              {% else %}
                {% set prijzen_sorted = prijzen | sort(attribute='value') %}
                {% set ns.goedkope_starttijden = prijzen_sorted[:aantal_goedkoop] | map(attribute='start') | list %}
                {% if aantal_duur > 0 %}
                  {% set ns.dure_starttijden = prijzen_sorted[-aantal_duur:] | map(attribute='start') | list %}
                {% else %}
                  {% set ns.dure_starttijden = [] %}
                {% endif %}
              {% endif %}
              [
              {% for uur in prijzen %}
                {
                  "start": "{{ uur.start }}",
                  "end": "{{ uur.end }}",
                  "value": {{ uur.value }},
                  "goedkoop": "{{ 'Yes' if uur.start in ns.goedkope_starttijden else 'No' }}",
                  "duur": "{{ 'Yes' if uur.start in ns.dure_starttijden else 'No' }}"
                }{% if not loop.last %},{% endif %}
              {% endfor %}
              ]
            {% else %}
              []
            {% endif %}
          raw_tomorrow: >
            {% set prijzen = state_attr('sensor.dynamic_nordpool', 'raw_tomorrow') %}
            {% set handmatige = states('input_text.dynamic_manual_period_tomorrow') %}
            {% set aantal_goedkoop = states('input_number.dynamic_lowest_price_x_period_tomorrow') | int %}
            {% set aantal_duur = states('input_number.dynamic_highest_price_x_period_tomorrow') | int %}
            {% set kwartier = is_state('input_boolean.dynamic_setting_15_minute_interval', 'on') %}
            {% set default_delta = timedelta(minutes=15) if kwartier else timedelta(hours=1) %}
            {% if prijzen is iterable %}
              {% set ns = namespace(goedkope_starttijden=[], dure_starttijden=[]) %}
              {% if handmatige | length > 0 %}
                {% for item in handmatige.split(';') %}
                  {% set type = item[0] %}
                  {% set tijden = item[1:] %}
                  {% if '-' in tijden %}
                    {% set start_str = tijden.split('-')[0] %}
                    {% set eind_str = tijden.split('-')[1] %}
                    {% set range_block = True %}
                  {% else %}
                    {% set start_str = tijden %}
                    {% set range_block = False %}
                  {% endif %}
                  {% for uur in prijzen %}
                    {% set dt_start = uur.start | as_datetime %}
                    {% set dt_end = uur.end | as_datetime %}
                    {% set dag = dt_start.strftime('%Y-%m-%d') %}
                    {% set tz = dt_start.tzinfo %}
                    {% set start_dt = strptime(dag ~ ' ' ~ start_str, '%Y-%m-%d %H:%M').replace(tzinfo=tz) %}
                    {% if range_block %}
                      {% set eind_dt = strptime(dag ~ ' ' ~ eind_str, '%Y-%m-%d %H:%M').replace(tzinfo=tz) %}
                      {% if eind_dt <= start_dt %}
                        {% set eind_dt = eind_dt + timedelta(days=1) %}
                      {% endif %}
                      {% set eind_dt = eind_dt + timedelta(seconds=1) %}
                    {% else %}
                      {% set eind_dt = start_dt + default_delta %}
                    {% endif %}
                    {% if dt_start < eind_dt and dt_end > start_dt %}
                      {% if type == 'G' %}
                        {% set ns.goedkope_starttijden = ns.goedkope_starttijden + [uur.start] %}
                      {% elif type == 'D' %}
                        {% set ns.dure_starttijden = ns.dure_starttijden + [uur.start] %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                {% endfor %}
              {% else %}
                {% set prijzen_sorted = prijzen | sort(attribute='value') %}
                {% set ns.goedkope_starttijden = prijzen_sorted[:aantal_goedkoop] | map(attribute='start') | list %}
                {% if aantal_duur > 0 %}
                  {% set ns.dure_starttijden = prijzen_sorted[-aantal_duur:] | map(attribute='start') | list %}
                {% else %}
                  {% set ns.dure_starttijden = [] %}
                {% endif %}
              {% endif %}
              [
              {% for uur in prijzen %}
                {
                  "start": "{{ uur.start }}",
                  "end": "{{ uur.end }}",
                  "value": {{ uur.value }},
                  "goedkoop": "{{ 'Yes' if uur.start in ns.goedkope_starttijden else 'No' }}",
                  "duur": "{{ 'Yes' if uur.start in ns.dure_starttijden else 'No' }}"
                }{% if not loop.last %},{% endif %}
              {% endfor %}
              ]
            {% else %}
              []
            {% endif %}
          duurste_na_eerste_goedkope: >
            {% set prijzen = state_attr('sensor.dynamic_lowest_price_period','raw_today') or [] %}
            {% set goedkope = prijzen
              | selectattr('goedkoop','eq','Yes')
              | map(attribute='start')
              | map('as_datetime')
              | select('!=', None)
              | sort
              | list %}
            {% set eerste_goedkoop = goedkope | first if goedkope else none %}
            {% if prijzen and eerste_goedkoop %}
              [
              {% for uur in prijzen
                if uur.duur == 'Yes'
                and (uur.start | as_datetime > eerste_goedkoop)
              %}
                {
                  "start": "{{ uur.start }}",
                  "end": "{{ uur.end }}",
                  "value": {{ uur.value }},
                  "goedkoop": "{{ uur.goedkoop }}",
                  "duur": "Yes"
                }{% if not loop.last %},{% endif %}
              {% endfor %}
              ]
            {% else %}
              []
            {% endif %}

          duurste_na_eerste_goedkope_tomorrow: >
            {% set prijzen = state_attr('sensor.dynamic_lowest_price_period','raw_tomorrow') or [] %}
            {% set goedkope = prijzen
              | selectattr('goedkoop','eq','Yes')
              | map(attribute='start')
              | map('as_datetime')
              | select('!=', None)
              | sort
              | list %}
            {% set eerste_goedkoop = goedkope | first if goedkope else none %}
            {% if prijzen and eerste_goedkoop %}
              [
              {% for uur in prijzen
                if uur.duur == 'Yes'
                and (uur.start | as_datetime > eerste_goedkoop)
              %}
                {
                  "start": "{{ uur.start }}",
                  "end": "{{ uur.end }}",
                  "value": {{ uur.value }},
                  "goedkoop": "{{ uur.goedkoop }}",
                  "duur": "Yes"
                }{% if not loop.last %},{% endif %}
              {% endfor %}
              ]
            {% else %}
              []
            {% endif %}

      - name: "Zendure Export Efficiency"
        unique_id: zendure_export_efficiency
        unit_of_measurement: "%"
        state_class: measurement
        icon: mdi:percent-box
        state: >
          {% set ac = states('sensor.zendure_power_export') | float(0) %}
          {% set dc = states('sensor.zendure_power_export_dc') | float(0) %}
          {% if ac > 0 and dc > 0 %}
            {% set efficiency = (ac / dc) * 100 %}
            {{ [0, [efficiency, 100] | min] | max | round(1) }}
          {% else %}
            0
          {% endif %}

      - name: "Zendure Import Efficiency"
        unique_id: zendure_import_efficiency
        unit_of_measurement: "%"
        state_class: measurement
        icon: mdi:percent-box
        state: >
          {% set ac = states('sensor.zendure_power_import') | float(0) %}
          {% set dc = states('sensor.zendure_power_import_dc') | float(0) %}
          {% if ac > 0 and dc > 0 %}
            {% set efficiency = (dc / ac) * 100 %}
            {{ [0, [efficiency, 100] | min] | max | round(1) }}
          {% else %}
            0
          {% endif %}

      - name: "Zendure Indication Available Energy"
        unique_id: zendure_indication_available_energy
        unit_of_measurement: "kWh"
        device_class: energy
        icon: mdi:lightning-bolt-circle
        state: >
          {% set laad = states('sensor.zendure_total_state_of_charge') | float(0) %}
          {% set min_laad = states('sensor.zendure_minimum_state_of_charge') | float(0) %}
          {% set max_laad = states('sensor.zendure_maximum_state_of_charge') | float(100) %}
          {% set totale_capaciteit = states('sensor.zendure_total_capacity') | float(0) %}

          {% set export_eff = states('sensor.zendure_export_efficiency_24h') | float(0) %}
          {% set rte_totaal = states('sensor.zendure_rte_total') | float(0) %}

          {% set effectief_laad = [laad, max_laad] | min %}
          {% set rte_export_equivalent = (rte_totaal / 100) ** 0.5 * 100 %}

          {% if export_eff < 70 %}
            {% set effectieve_eff = rte_export_equivalent %}
          {% else %}
            {% set effectieve_eff = export_eff %}
          {% endif %}

          {% set effectieve_eff = [effectieve_eff, 100] | min / 100 %}
          {% set bruikbaar_percentage = effectief_laad - min_laad %}

          {% if bruikbaar_percentage <= 0 %}
            0
          {% else %}
            {{ (bruikbaar_percentage / 100 * totale_capaciteit * effectieve_eff) | round(2) }}
          {% endif %}
        attributes:
          total_capacity_kwh: >
            {{ states('sensor.zendure_total_capacity') }}
          indication_available_energy_(without_loss): >
            {% set laad = states('sensor.zendure_total_state_of_charge') | float(0) %}
            {% set min_laad = states('sensor.zendure_minimum_state_of_charge') | float(0) %}
            {% set max_laad = states('sensor.zendure_maximum_state_of_charge') | float(100) %}
            {% set totale_capaciteit = states('sensor.zendure_total_capacity') | float(0) %}

            {% set effectief_laad = [laad, max_laad] | min %}
            {% set bruikbaar_percentage = effectief_laad - min_laad %}

            {% if bruikbaar_percentage <= 0 %}
              0
            {% else %}
              {{ (bruikbaar_percentage / 100 * totale_capaciteit) | round(2) }}
            {% endif %}
          usable_percentage: >
            {% set laad = states('sensor.zendure_total_state_of_charge') | float(0) %}
            {% set min_laad = states('sensor.zendure_minimum_state_of_charge') | float(0) %}
            {% set max_laad = states('sensor.zendure_maximum_state_of_charge') | float(100) %}
            {{ ([laad, max_laad] | min - min_laad) | round(1) }} %
          used_export_efficiency: >
            {% set export_eff = states('sensor.zendure_export_efficiency_24h') | float(0) %}
            {% set rte_totaal = states('sensor.zendure_rte_total') | float(0) %}
            {% set rte_export_equivalent = (rte_totaal / 100) ** 0.5 * 100 %}
            {% if export_eff < 70 %}
              {{ rte_export_equivalent | round(1) }} %
            {% else %}
              {{ export_eff | round(1) }} %
            {% endif %}
          source_export_efficiency: >
            {% set export_eff = states('sensor.zendure_export_efficiency_24h') | float(0) %}
            {% if export_eff < 70 %}
              RTE total (√RTE × 100)
            {% else %}
              Export efficiency 24h average
            {% endif %}

      - name: "Zendure Indication Required Energy"
        unique_id: zendure_indication_required_energy
        unit_of_measurement: "kWh"
        device_class: energy
        icon: mdi:lightning-bolt-circle
        state: >
          {% set laad = states('sensor.zendure_total_state_of_charge') | float(0) %}
          {% set max_laad = states('sensor.zendure_maximum_state_of_charge') | float(100) %}
          {% set totale_capaciteit = states('sensor.zendure_total_capacity') | float(0) %}

          {% set import_eff = states('sensor.zendure_import_efficiency_24h') | float(0) %}
          {% set rte_totaal = states('sensor.zendure_rte_total') | float(0) %}

          {% set effectief_laad = [laad, max_laad] | min %}
          {% set rte_import_equivalent = (rte_totaal / 100) ** 0.5 * 100 %}

          {% if import_eff < 70 %}
            {% set effectieve_eff = rte_import_equivalent %}
          {% else %}
            {% set effectieve_eff = import_eff %}
          {% endif %}

          {% set effectieve_eff = [effectieve_eff, 100] | min / 100 %}
          {% set benodigd_percentage = max_laad - effectief_laad %}

          {% if benodigd_percentage <= 0 %}
            0
          {% else %}
            {{ (benodigd_percentage / 100 * totale_capaciteit / effectieve_eff) | round(2) }}
          {% endif %}
        attributes:
          total_capacity_kwh: >
            {{ states('sensor.zendure_total_capacity') }}
          indication_required_energy_(without_loss): >
            {% set laad = states('sensor.zendure_total_state_of_charge') | float(0) %}
            {% set max_laad = states('sensor.zendure_maximum_state_of_charge') | float(100) %}
            {% set totale_capaciteit = states('sensor.zendure_total_capacity') | float(0) %}

            {% set effectief_laad = [laad, max_laad] | min %}
            {% set benodigd_percentage = max_laad - effectief_laad %}

            {% if benodigd_percentage <= 0 %}
              0
            {% else %}
              {{ (benodigd_percentage / 100 * totale_capaciteit) | round(2) }}
            {% endif %}
          required_percentage: >
            {% set laad = states('sensor.zendure_total_state_of_charge') | float(0) %}
            {% set max_laad = states('sensor.zendure_maximum_state_of_charge') | float(100) %}
            {{ ([max_laad - [laad, max_laad] | min, 0] | max) | round(1) }} %
          used_import_efficiency: >
            {% set import_eff = states('sensor.zendure_import_efficiency_24h') | float(0) %}
            {% set rte_totaal = states('sensor.zendure_rte_total') | float(0) %}
            {% set rte_import_equivalent = (rte_totaal / 100) ** 0.5 * 100 %}
            {% if import_eff < 70 %}
              {{ rte_import_equivalent | round(1) }} %
            {% else %}
              {{ import_eff | round(1) }} %
            {% endif %}
          source_import_efficiency: >
            {% set import_eff = states('sensor.zendure_import_efficiency_24h') | float(0) %}
            {% if import_eff < 70 %}
              RTE total (√RTE × 100)
            {% else %}
              Import efficiency 24h average
            {% endif %}

      - name: "Zendure RTE Total"
        unique_id: zendure_rte_total
        unit_of_measurement: "%"
        state_class: measurement
        icon: mdi:percent-box
        state: >
          {% set energy_import = states('sensor.zendure_energy_import') | float(0) %}
          {% set energy_export = states('sensor.zendure_energy_export') | float(0) %}
          {% if energy_import > 0 %}
            {% set percentage = ((energy_export / energy_import) * 100) | round(2) %}
            {% if 0 <= percentage <= 100 %}
              {{ percentage }}
            {% else %}
              0
            {% endif %}
          {% else %}
            0
          {% endif %}

rest:
  - resource_template: "http://{{ states('input_text.zendure_setting_ip_address') }}/properties/report"
    scan_interval: 1
    sensor:
      - name: "Zendure PV Export Mode"
        unique_id: zendure_pv_export_mode
        icon: mdi:solar-panel
        value_template: >
          {% set states = {1: "Allow", 2: "Forbidden"} %}
          {% set packState = value_json.get('properties', {}).get('gridReverse', 0) | int %}
          {{ states.get(packState, "Onbekend") }}

      - name: "Zendure Battery 1 Cell Balance Status"
        unique_id: zendure_battery_1_cell_balance_status
        icon: mdi:battery-heart-variant
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% set packdata = value_json['packData'] %}
          {% if not packdata %}
            unknown
          {% elif volgorde_raw in ['unknown','unavailable',''] %}
            {% set p = packdata[0] %}
            {% set d = p['maxVol'] - p['minVol'] %}
            {{ 'Excellent' if d <= 2 else
               'Good' if d <= 5 else
               'Slight Imbalance' if d <= 8 else
               'Imbalanced' }}
          {% else %}
            {% set idx = (volgorde_raw.split(';')[0] | int) - 1 %}
            {% if idx < packdata | length %}
              {% set p = packdata[idx] %}
              {% set d = p['maxVol'] - p['minVol'] %}
              {{ 'Excellent' if d <= 2 else
                 'Good' if d <= 5 else
                 'Slight Imbalance' if d <= 8 else
                 'Imbalanced' }}
            {% else %}
              unknown
            {% endif %}
          {% endif %}

      - name: "Zendure Battery 2 Cell Balance Status"
        unique_id: zendure_battery_2_cell_balance_status
        icon: mdi:battery-heart-variant
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% set packdata = value_json['packData'] %}
          {% if not packdata %}
            unknown
          {% elif volgorde_raw in ['unknown','unavailable',''] %}
            {% set p = packdata[1] %}
            {% set d = p['maxVol'] - p['minVol'] %}
            {{ 'Excellent' if d <= 2 else
               'Good' if d <= 5 else
               'Slight Imbalance' if d <= 8 else
               'Imbalanced' }}
          {% else %}
            {% set idx = (volgorde_raw.split(';')[1] | int) - 1 %}
            {% if idx < packdata | length %}
              {% set p = packdata[idx] %}
              {% set d = p['maxVol'] - p['minVol'] %}
              {{ 'Excellent' if d <= 2 else
                 'Good' if d <= 5 else
                 'Slight Imbalance' if d <= 8 else
                 'Imbalanced' }}
            {% else %}
              unknown
            {% endif %}
          {% endif %}

      - name: "Zendure Battery 3 Cell Balance Status"
        unique_id: zendure_battery_3_cell_balance_status
        icon: mdi:battery-heart-variant
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% set packdata = value_json['packData'] %}
          {% if not packdata %}
            unknown
          {% elif volgorde_raw in ['unknown','unavailable',''] %}
            {% set p = packdata[2] %}
            {% set d = p['maxVol'] - p['minVol'] %}
            {{ 'Excellent' if d <= 2 else
               'Good' if d <= 5 else
               'Slight Imbalance' if d <= 8 else
               'Imbalanced' }}
          {% else %}
            {% set idx = (volgorde_raw.split(';')[2] | int) - 1 %}
            {% if idx < packdata | length %}
              {% set p = packdata[idx] %}
              {% set d = p['maxVol'] - p['minVol'] %}
              {{ 'Excellent' if d <= 2 else
                 'Good' if d <= 5 else
                 'Slight Imbalance' if d <= 8 else
                 'Imbalanced' }}
            {% else %}
              unknown
            {% endif %}
          {% endif %}

      - name: "Zendure Battery 4 Cell Balance Status"
        unique_id: zendure_battery_4_cell_balance_status
        icon: mdi:battery-heart-variant
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% set packdata = value_json['packData'] %}
          {% if not packdata %}
            unknown
          {% elif volgorde_raw in ['unknown','unavailable',''] %}
            {% set p = packdata[3] %}
            {% set d = p['maxVol'] - p['minVol'] %}
            {{ 'Excellent' if d <= 2 else
               'Good' if d <= 5 else
               'Slight Imbalance' if d <= 8 else
               'Imbalanced' }}
          {% else %}
            {% set idx = (volgorde_raw.split(';')[3] | int) - 1 %}
            {% if idx < packdata | length %}
              {% set p = packdata[idx] %}
              {% set d = p['maxVol'] - p['minVol'] %}
              {{ 'Excellent' if d <= 2 else
                 'Good' if d <= 5 else
                 'Slight Imbalance' if d <= 8 else
                 'Imbalanced' }}
            {% else %}
              unknown
            {% endif %}
          {% endif %}

      - name: "Zendure Battery 5 Cell Balance Status"
        unique_id: zendure_battery_5_cell_balance_status
        icon: mdi:battery-heart-variant
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% set packdata = value_json['packData'] %}
          {% if not packdata %}
            unknown
          {% elif volgorde_raw in ['unknown','unavailable',''] %}
            {% set p = packdata[4] %}
            {% set d = p['maxVol'] - p['minVol'] %}
            {{ 'Excellent' if d <= 2 else
               'Good' if d <= 5 else
               'Slight Imbalance' if d <= 8 else
               'Imbalanced' }}
          {% else %}
            {% set idx = (volgorde_raw.split(';')[4] | int) - 1 %}
            {% if idx < packdata | length %}
              {% set p = packdata[idx] %}
              {% set d = p['maxVol'] - p['minVol'] %}
              {{ 'Excellent' if d <= 2 else
                 'Good' if d <= 5 else
                 'Slight Imbalance' if d <= 8 else
                 'Imbalanced' }}
            {% else %}
              unknown
            {% endif %}
          {% endif %}

      - name: "Zendure Battery 6 Cell Balance Status"
        unique_id: zendure_battery_6_cell_balance_status
        icon: mdi:battery-heart-variant
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% set packdata = value_json['packData'] %}
          {% if not packdata %}
            unknown
          {% elif volgorde_raw in ['unknown','unavailable',''] %}
            {% set p = packdata[5] %}
            {% set d = p['maxVol'] - p['minVol'] %}
            {{ 'Excellent' if d <= 2 else
               'Good' if d <= 5 else
               'Slight Imbalance' if d <= 8 else
               'Imbalanced' }}
          {% else %}
            {% set idx = (volgorde_raw.split(';')[5] | int) - 1 %}
            {% if idx < packdata | length %}
              {% set p = packdata[idx] %}
              {% set d = p['maxVol'] - p['minVol'] %}
              {{ 'Excellent' if d <= 2 else
                 'Good' if d <= 5 else
                 'Slight Imbalance' if d <= 8 else
                 'Imbalanced' }}
            {% else %}
              unknown
            {% endif %}
          {% endif %}

      - name: "Zendure PV Passthrough"
        value_template: >
          {% set solar = value_json['properties']['solarInputPower'] | float(0) %}
          {% set grid_off = value_json.get('properties', {}).get('gridOffPower', 0) | float(0) %}
          {% set pv_active = (solar > 0) or (grid_off < 0) %}
          {% set states = {0: "Battery", 2: "Home"} %}
          {% set packState = value_json['properties']['pass'] | int %}
          {{ states.get(packState, "Onbekend") if pv_active else "PV Offline" }}
        unique_id: zendure_pv_passthrough
        icon: mdi:arrow-decision

      - name: "Zendure Power PV To Home"
        value_template: >
          {% set pv = value_json['properties']['pvStatus'] | int(0) %}
          {% set solar = value_json['properties']['solarInputPower'] | float(0) %}
          {% set grid_off = value_json.get('properties', {}).get('gridOffPower', 0) | float(0) %}
          {% set pv_active = (pv == 1) or (solar > 0) or (grid_off < 0) %}
          {% set home = value_json['properties']['outputHomePower'] | float(0) %}
          {% set pack_in = value_json['properties']['packInputPower'] | float(0) %}
          {{ (home - pack_in) | round(1) if pv_active else 0 }}
        unique_id: zendure_power_pv_to_home
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power

      - name: "Zendure Power PV Total"
        value_template: >
          {% set pv = value_json['properties']['pvStatus'] | int(0) %}
          {% set solar = value_json['properties']['solarInputPower'] | float(0) %}
          {% set grid_off = value_json.get('properties', {}).get('gridOffPower', 0) | float(0) %}
          {% set pv_active = (pv == 1) or (solar > 0) or (grid_off < 0) %}
          {{ (solar + (0 - grid_off if grid_off < 0 else 0)) if pv_active else 0 }}
        unique_id: zendure_power_pv_total
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power

      - name: "Zendure Power PV To Battery"
        value_template: >
          {% set pv = value_json['properties']['pvStatus'] | int(0) %}
          {% set solar = value_json['properties']['solarInputPower'] | float(0) %}
          {% set grid_off = value_json.get('properties', {}).get('gridOffPower', 0) | float(0) %}
          {% set pv_active = (pv == 1) or (solar > 0) or (grid_off < 0) %}
          {% set pack = value_json['properties']['outputPackPower'] | float(0) %}
          {% set grid = value_json['properties']['gridInputPower'] | float(0) %}
          {{ (pack - grid) | round(1) if pv_active else 0 }}
        unique_id: zendure_power_pv_to_battery
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power

      - name: "Zendure Power From Home"
        value_template: >
          {% set grid_in = value_json['properties']['gridInputPower'] | float(0) %}
          {% set grid_off = value_json.get('properties', {}).get('gridOffPower', 0) | float(0) %}
          {{ (grid_in - (grid_off if grid_off > 0 else 0)) | round(1) }}
        unique_id: zendure_power_from_home
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power

      - name: "Zendure Power To Home"
        value_template: >
          {% set pack_out = value_json['properties']['packInputPower'] | float(0) %}
          {{ pack_out | round(1) }}
        unique_id: zendure_power_to_home
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power

      - name: "Zendure Power Offgrid Outlet 1"
        value_template: "{{ value_json.get('properties', {}).get('gridOffPower', 0) | int }}"
        unique_id: zendure_power_offgrid_outlet_1
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power
        icon: mdi:power-plug

      - name: "Zendure Offgrid Mode"
        value_template: >
          {% set states = {0: "Normal", 1: "Eco", 2: "Turned Off"} %}
          {% set packState = value_json.get('properties', {}).get('gridOffMode', 2) | int %}
          {{ states.get(packState, "Onbekend") }}
        unique_id: zendure_offgrid_mode
        icon: >
          {% if this.state == 'Normal' %}
           mdi:power-plug
          {% elif this.state == 'Eco' %}
           mdi:power-plug
          {% elif this.state == 'Turned Off' %}
           mdi:power-plug-off
          {% else %}
           mdi:power-plug-outline
          {% endif %}

      - name: "Zendure Product Model"
        unique_id: zendure_product_model
        value_template: "{{ value_json.product }}"
        icon: mdi:identifier

      - name: "Zendure Total Capacity"
        unique_id: zendure_total_capacity
        unit_of_measurement: "kWh"
        icon: mdi:flip-vertical
        value_template: >
          {% set product = value_json.product %}
          {% if product in ["solarFlow4000MixAC+", "solarFlow4000MixPro", "solarFlow3000MixAC+"] %}
            {% set mapping = {
              70: 8.00
            } %}
          {% else %}
            {% set mapping = {
              5: 2.88,
              70: 1.92,
              300: 1.92,
              250: 0.96,
              350: 2.88,
              500: 2.40
            } %}
          {% endif %}
          {% set packs = value_json.packData %}
          {% if packs %}
            {% set ns = namespace(total=0) %}
            {% for pack in packs %}
              {% set type = pack.packType | int %}
              {% if type in mapping %}
                {% set ns.total = ns.total + mapping[type] %}
              {% endif %}
            {% endfor %}
            {{ ns.total | round(2) }}
          {% else %}
            0
          {% endif %}

      - name: "Zendure Inverter Max. Charge Power"
        value_template: "{{ value_json['properties']['chargeMaxLimit'] }}"
        unique_id: zendure_inverter_max_charge_power
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power

      - name: "Zendure Inverter Max. Discharge Power"
        value_template: "{{ value_json['properties']['inverseMaxPower'] }}"
        unique_id: zendure_inverter_max_discharge_power
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power

      - name: "Zendure SOC-limit Status"
        value_template: >
          {% set states = {0: "Normal Operation", 1: "Charging Limit Reached", 2: "Discharge Limit Reached", 16: "Normal Operation", 17: "Charging Limit Reached", 18: "Discharge Limit Reached"} %}
          {% set packState = value_json['properties']['socLimit'] | int %}
          {{ states.get(packState, "Onbekend") }}
        unique_id: zendure_soc_limit_status
        icon: >
          {% if this.state == 'Normal Operation' %}
           mdi:battery-medium
          {% elif this.state == 'Charging Limit Reached' %}
           mdi:battery-high
          {% elif this.state == 'Discharge Limit Reached' %}
           mdi:battery-low
          {% else %}
           mdi:battery-outline
          {% endif %}

      - name: "Zendure Battery Serial Numbers"
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% set packdata = value_json['packData'] %}
          {% set ns = namespace(sn_list=[]) %}
          {% if not packdata %}
            unknown
          {% elif volgorde_raw in ['unknown', 'unavailable', ''] %}
            {% for pack in packdata %}
              {% set ns.sn_list = ns.sn_list + [ pack['sn'] ] %}
            {% endfor %}
          {% else %}
            {% set volgorde = volgorde_raw.split(';') %}
            {% for v in volgorde %}
              {% set idx = (v | int) - 1 %}
              {% if idx >= 0 and idx < packdata | length %}
                {% set ns.sn_list = ns.sn_list + [ packdata[idx]['sn'] ] %}
              {% endif %}
            {% endfor %}
          {% endif %}
          {{ ns.sn_list | join('\n') }}
        unique_id: zendure_battery_serial_numbers
        icon: mdi:identifier

      - name: "Zendure Battery 1 State Of Charge"
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% set packdata = value_json['packData'] %}
          {% if not packdata %}
            unknown
          {% elif volgorde_raw in ['unknown', 'unavailable', ''] %}
            {{ packdata[0]['socLevel'] }}
          {% else %}
            {% set idx = (volgorde_raw.split(';')[0] | int) - 1 %}
            {{ packdata[idx]['socLevel'] if idx < packdata | length else 'unknown' }}
          {% endif %}
        unique_id: "zendure_battery_1_state_of_charge"
        device_class: battery
        unit_of_measurement: "%"
        state_class: measurement

      - name: "Zendure Battery 2 State Of Charge"
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% set packdata = value_json['packData'] %}
          {% if not packdata %}
            unknown
          {% elif volgorde_raw in ['unknown', 'unavailable', ''] %}
            {{ packdata[1]['socLevel'] }}
          {% else %}
            {% set idx = (volgorde_raw.split(';')[1] | int) - 1 %}
            {{ packdata[idx]['socLevel'] if idx < packdata | length else 'unknown' }}
          {% endif %}
        unique_id: "zendure_battery_2_state_of_charge"
        device_class: battery
        unit_of_measurement: "%"
        state_class: measurement

      - name: "Zendure Battery 3 State Of Charge"
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% set packdata = value_json['packData'] %}
          {% if not packdata %}
            unknown
          {% elif volgorde_raw in ['unknown', 'unavailable', ''] %}
            {{ packdata[2]['socLevel'] }}
          {% else %}
            {% set idx = (volgorde_raw.split(';')[2] | int) - 1 %}
            {{ packdata[idx]['socLevel'] if idx < packdata | length else 'unknown' }}
          {% endif %}
        unique_id: "zendure_battery_3_state_of_charge"
        device_class: battery
        unit_of_measurement: "%"
        state_class: measurement

      - name: "Zendure Battery 4 State Of Charge"
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% set packdata = value_json['packData'] %}
          {% if not packdata %}
            unknown
          {% elif volgorde_raw in ['unknown', 'unavailable', ''] %}
            {{ packdata[3]['socLevel'] }}
          {% else %}
            {% set idx = (volgorde_raw.split(';')[3] | int) - 1 %}
            {{ packdata[idx]['socLevel'] if idx < packdata | length else 'unknown' }}
          {% endif %}
        unique_id: "zendure_battery_4_state_of_charge"
        device_class: battery
        unit_of_measurement: "%"
        state_class: measurement

      - name: "Zendure Battery 5 State Of Charge"
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% set packdata = value_json['packData'] %}
          {% if not packdata %}
            unknown
          {% elif volgorde_raw in ['unknown', 'unavailable', ''] %}
            {{ packdata[4]['socLevel'] }}
          {% else %}
            {% set idx = (volgorde_raw.split(';')[4] | int) - 1 %}
            {{ packdata[idx]['socLevel'] if idx < packdata | length else 'unknown' }}
          {% endif %}
        unique_id: "zendure_battery_5_state_of_charge"
        device_class: battery
        unit_of_measurement: "%"
        state_class: measurement

      - name: "Zendure Battery 6 State Of Charge"
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% set packdata = value_json['packData'] %}
          {% if not packdata %}
            unknown
          {% elif volgorde_raw in ['unknown', 'unavailable', ''] %}
            {{ packdata[5]['socLevel'] }}
          {% else %}
            {% set idx = (volgorde_raw.split(';')[5] | int) - 1 %}
            {{ packdata[idx]['socLevel'] if idx < packdata | length else 'unknown' }}
          {% endif %}
        unique_id: "zendure_battery_6_state_of_charge"
        device_class: battery
        unit_of_measurement: "%"
        state_class: measurement

      - name: "Zendure Relay Mode"
        unique_id: zendure_relay_mode
        value_template: >
          {% set states = {1: "Oplaadstand", 2: "Ontlaadstand"} %}
          {% set packState = value_json['properties']['acMode'] | int %}
          {{ states.get(packState, "Onbekend") }}
        icon: mdi:swap-vertical-bold

      - name: "Zendure Serial Number"
        unique_id: zendure_serial_number
        value_template: "{{ value_json.sn }}"
        icon: mdi:identifier

      - name: "Zendure Power Import DC"
        unique_id: zendure_power_import_dc
        value_template: >
          {% set ns = namespace(total=0) %}
          {% for pack in value_json.packData %}
           {% if pack.state == 1 %}
           {% set ns.total = ns.total + (pack.power | int) %}
          {% endif %}
          {% endfor %}
          {{ ns.total }}
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power

      - name: "Zendure Power Export DC"
        unique_id: zendure_power_export_dc
        value_template: >
          {% set ns = namespace(total=0) %}
          {% for pack in value_json.packData %}
           {% if pack.state == 2 %}
           {% set ns.total = ns.total + (pack.power | int) %}
          {% endif %}
          {% endfor %}
          {{ ns.total }}
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power

      - name: "Zendure Power Export"
        value_template: "{{ value_json['properties']['packInputPower'] | int }}"
        unique_id: zendure_power_export
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power

      - name: "Zendure Power Import"
        value_template: "{{ (value_json['properties']['outputPackPower'] | int) }}"
        unique_id: zendure_power_import
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power

      - name: "Zendure WIFI Signal Strength"
        value_template: >
          {% set rssi = value_json['properties']['rssi'] | int(-100) %}
          {% if rssi >= -60 %}
            Excellent
          {% elif rssi >= -70 %}
            Good
          {% elif rssi >= -80 %}
            Weak
          {% else %}
            Bad
          {% endif %}
        unique_id: zendure_wifi_signal_strength
        icon: mdi:wifi

      - name: "Zendure Battery 1 Temperature"
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% if volgorde_raw in ['unknown', 'unavailable', ''] %}
            {% set pack_index = 0 %}
          {% else %}
            {% set volgorde = volgorde_raw.split(';') %}
            {% set pack_index = (volgorde[0] | int) - 1 %}
          {% endif %}
          {% set maxTemp = value_json['packData'][pack_index]['maxTemp'] | int %}
          {{ (maxTemp - 2731) / 10.0 }}
        unique_id: zendure_battery_1_temperature
        unit_of_measurement: "°C"
        state_class: measurement
        device_class: temperature
        icon: mdi:thermometer

      - name: "Zendure Battery 2 Temperature"
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% if volgorde_raw in ['unknown', 'unavailable', ''] %}
            {% set pack_index = 1 %}
          {% else %}
            {% set volgorde = volgorde_raw.split(';') %}
            {% set pack_index = (volgorde[1] | int) - 1 %}
          {% endif %}
          {% set maxTemp = value_json['packData'][pack_index]['maxTemp'] | int %}
          {{ (maxTemp - 2731) / 10.0 }}
        unique_id: zendure_battery_2_temperature
        unit_of_measurement: "°C"
        state_class: measurement
        device_class: temperature
        icon: mdi:thermometer

      - name: "Zendure Battery 3 Temperature"
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% if volgorde_raw in ['unknown', 'unavailable', ''] %}
            {% set pack_index = 2 %}
          {% else %}
            {% set volgorde = volgorde_raw.split(';') %}
            {% set pack_index = (volgorde[2] | int) - 1 %}
          {% endif %}
          {% set maxTemp = value_json['packData'][pack_index]['maxTemp'] | int %}
          {{ (maxTemp - 2731) / 10.0 }}
        unique_id: zendure_battery_3_temperature
        unit_of_measurement: "°C"
        state_class: measurement
        device_class: temperature
        icon: mdi:thermometer

      - name: "Zendure Battery 4 Temperature"
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% if volgorde_raw in ['unknown', 'unavailable', ''] %}
            {% set pack_index = 3 %}
          {% else %}
            {% set volgorde = volgorde_raw.split(';') %}
            {% set pack_index = (volgorde[3] | int) - 1 %}
          {% endif %}
          {% set maxTemp = value_json['packData'][pack_index]['maxTemp'] | int %}
          {{ (maxTemp - 2731) / 10.0 }}
        unique_id: zendure_battery_4_temperature
        unit_of_measurement: "°C"
        state_class: measurement
        device_class: temperature
        icon: mdi:thermometer

      - name: "Zendure Battery 5 Temperature"
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% if volgorde_raw in ['unknown', 'unavailable', ''] %}
            {% set pack_index = 4 %}
          {% else %}
            {% set volgorde = volgorde_raw.split(';') %}
            {% set pack_index = (volgorde[4] | int) - 1 %}
          {% endif %}
          {% set maxTemp = value_json['packData'][pack_index]['maxTemp'] | int %}
          {{ (maxTemp - 2731) / 10.0 }}
        unique_id: zendure_battery_5_temperature
        unit_of_measurement: "°C"
        state_class: measurement
        device_class: temperature
        icon: mdi:thermometer

      - name: "Zendure Battery 6 Temperature"
        value_template: >
          {% set volgorde_raw = states('input_text.zendure_setting_battery_order') %}
          {% if volgorde_raw in ['unknown', 'unavailable', ''] %}
            {% set pack_index = 5 %}
          {% else %}
            {% set volgorde = volgorde_raw.split(';') %}
            {% set pack_index = (volgorde[5] | int) - 1 %}
          {% endif %}
          {% set maxTemp = value_json['packData'][pack_index]['maxTemp'] | int %}
          {{ (maxTemp - 2731) / 10.0 }}
        unique_id: zendure_battery_6_temperature
        unit_of_measurement: "°C"
        state_class: measurement
        device_class: temperature
        icon: mdi:thermometer

      - name: "Zendure Power"
        value_template: >
          {% set pv = value_json['properties']['pvStatus'] | int(0) %}
          {% set solar = value_json['properties']['solarInputPower'] | float(0) %}
          {% set grid_off = value_json.get('properties', {}).get('gridOffPower', 0) | float(0) %}
          {% set pv_active = (pv == 1) or (solar > 0) or (grid_off < 0) %}
          {% set pass = value_json['properties']['pass'] | int %}
          {% set ac_mode = value_json['properties']['acMode'] | int %}
          {% if not pv_active %}
            {% set opladen = value_json['properties']['outputPackPower'] | float(0) %}
            {% set ontladen = - (value_json['properties']['packInputPower'] | float(0)) %}
            {{ opladen if opladen != 0 else ontladen }}
          {% else %}
            {% set opladen = value_json['properties']['gridInputPower'] | float(0) %}
            {% set opladen_vol = value_json['properties']['outputPackPower'] | float(0) %}
            {% set ontladen = - (value_json['properties']['outputHomePower'] | float(0)) %}
            {% if pass == 2 %}
              {% set result = opladen_vol %}
            {% elif opladen != 0 %}
              {% set result = opladen %}
            {% else %}
              {% set result = ontladen %}
            {% endif %}
            {% if ac_mode == 1 and result < 0 %}
              0
            {% else %}
              {{ result }}
            {% endif %}
          {% endif %}
        unique_id: zendure_power
        icon: >
          {% set power = states('sensor.zendure_power') | float(0) %}
          {% if power > 0 %}
            mdi:battery-plus-variant
          {% elif power < 0 %}
            mdi:battery-minus-variant
          {% else %}
            mdi:battery-outline
          {% endif %}
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power

      - name: "Zendure Total Batteries"
        value_template: "{{ value_json['properties']['packNum'] }}"
        unique_id: zendure_total_batteries
        icon: mdi:flip-vertical

      - name: "Zendure Storage Mode"
        value_template: >
          {% set states = {1: "RAM Memory", 0: "Flash Memory"} %}
          {% set packState = value_json['properties']['smartMode'] | int %}
          {{ states.get(packState, "Onbekend") }}
        unique_id: zendure_storage_mode
        icon: mdi:floppy

      - name: "Zendure Battery Voltage"
        value_template: "{{ (value_json['properties']['BatVolt'] | float / 100) | round(2) }}"
        unique_id: zendure_battery_voltage
        unit_of_measurement: "V"
        state_class: measurement
        device_class: voltage
        icon: mdi:sine-wave

      - name: "Zendure Indication Remaining Discharge Time"
        value_template: >
          {% set vermogen = states('sensor.zendure_power') | float(0) %}
          {% set energie = states('sensor.zendure_indication_available_energy') | float(0) %}
          {% if vermogen < 0 %}
          {% set ontlaadvermogen_kw = (vermogen | abs) / 1000 %}
          {% if ontlaadvermogen_kw > 0 %}
            {% set uren = energie / ontlaadvermogen_kw %}
            {% set total_minutes = (uren * 60) | int %}
            {% set hours = total_minutes // 60 %}
            {% set minutes = total_minutes % 60 %}
            {{ hours }}h {{ minutes }}m
          {% else %}
            Unknown
          {% endif %}
          {% else %}
            Not Discharging
          {% endif %}
        icon: mdi:clock-time-eight-outline
        unique_id: zendure_indication_remaining_discharge_time

      - name: "Zendure Indication Remaining Charge Time"
        value_template: >
          {% set vermogen = states('sensor.zendure_power') | float(0) %}
          {% set energie = states('sensor.zendure_indication_required_energy') | float(0) %}
          {% set laad = states('sensor.zendure_total_state_of_charge') | float(0) %}
          {% set max_laad = states('sensor.zendure_maximum_state_of_charge') | float(100) %}
          {% if vermogen > 0 %}
           {% set oplaadvermogen_kw = vermogen / 1000 %}
           {% if oplaadvermogen_kw > 0 and energie > 0 %}
          {% set uren = energie / oplaadvermogen_kw %}
          {% set total_minutes = (uren * 60) | int %}
          {% set hours = total_minutes // 60 %}
          {% set minutes = total_minutes % 60 %}
          {{ hours }}h {{ minutes }}m
          {% else %}
           Unknown
          {% endif %}
          {% else %}
           Not Charging
           {% endif %}
        icon: mdi:clock-time-four-outline
        unique_id: zendure_indication_remaining_charge_time

      - name: "Zendure Inverter Temperature"
        value_template: >
          {% set maxTemp = value_json['properties']['hyperTmp'] | int %}
          {{ (maxTemp - 2731) / 10.0 }}
        unique_id: zendure_inverter_temperature
        unit_of_measurement: "°C"
        state_class: measurement
        device_class: temperature
        icon: mdi:thermometer

      - name: "Zendure Total State Of Charge"
        value_template: "{{ value_json['properties']['electricLevel'] }}"
        device_class: battery
        unit_of_measurement: "%"
        state_class: measurement
        unique_id: zendure_total_state_of_charge

      - name: "Zendure Minimum State Of Charge"
        value_template: "{{ (value_json['properties']['minSoc'] | int / 10) | int }}"
        device_class: battery
        unit_of_measurement: "%"
        state_class: measurement
        unique_id: zendure_minimum_state_of_charge
        icon: mdi:battery-high

      - name: "Zendure Maximum State Of Charge"
        value_template: "{{ (value_json['properties']['socSet'] | int / 10) | int }}"
        device_class: battery
        unit_of_measurement: "%"
        state_class: measurement
        unique_id: zendure_maximum_state_of_charge
        icon: mdi:battery-high

      - name: "Zendure Charging Mode"
        value_template: >
          {% set power = states('sensor.zendure_power') | float(0) %}
          {% if power > 0 %}
            Charging
          {% elif power < 0 %}
            Discharging
          {% else %}
            Standby
          {% endif %}
        unique_id: zendure_charging_mode
        icon: >
          {% set power = states('sensor.zendure_power') | float(0) %}
          {% if power > 0 %}
            mdi:battery-plus-variant
          {% elif power < 0 %}
            mdi:battery-minus-variant
          {% else %}
            mdi:battery-outline
          {% endif %}

      - name: "Zendure Error"
        value_template: >
          {% set states = {0: "No Notifications", 1: "Check Zendure APP"} %}
          {% set packState = value_json['properties']['is_error'] | int %}
          {{ states.get(packState, "Onbekend") }}
        unique_id: zendure_error
        icon: mdi:battery-alert

      - name: "Zendure Calibrating"
        value_template: >
          {% set states = {0: "No", 1: "Yes"} %}
          {% set packState = value_json['properties']['socStatus'] | int %}
          {{ states.get(packState, "Onbekend") }}
        unique_id: zendure_calibrating
        icon: mdi:battery-heart-variant

      - name: "Zendure Set Discharge Power"
        value_template: "{{ value_json['properties']['outputLimit'] }}"
        unique_id: zendure_set_discharge_power
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power

      - name: "Zendure Set Charge Power"
        value_template: "{{ value_json['properties']['inputLimit'] }}"
        unique_id: zendure_set_charge_power
        unit_of_measurement: "W"
        state_class: measurement
        device_class: power

  ####### START - Place here your Node-RED sensors from https://github.com/gast777/Zendure-zenSDK-proxy - START #######

  ####### END - Place here your Node-RED sensors from https://github.com/gast777/Zendure-zenSDK-proxy - END #######

  - resource_template: "http://{{ states('input_text.homewizard_setting_p1_ip_address') }}/api/v1/data"
    scan_interval: 1
    sensor:
      - name: "Homewizard P1 Power"
        value_template: "{{ value_json.active_power_w | float }}"
        unique_id: homewizard_p1_power
        unit_of_measurement: "W"
        device_class: power

rest_command:
  zendure_save_in_ram:
    url: http://{{ states('input_text.zendure_setting_ip_address') }}/properties/write
    method: POST
    headers:
      Content-Type: application/json
      Content-Encoding: identity
    payload: '{"sn":"{{sn}}","properties":{"smartMode": 1 }}'

  zendure_quick_charge:
    url: http://{{ states('input_text.zendure_setting_ip_address') }}/properties/write
    method: POST
    headers:
      Content-Type: application/json
      Content-Encoding: identity
    payload: '{"sn":"{{sn}}","properties":{"acMode": 1, "inputLimit": {{inputLimit}} }}'

  zendure_stop_with_charging:
    url: http://{{ states('input_text.zendure_setting_ip_address') }}/properties/write
    method: POST
    headers:
      Content-Type: application/json
      Content-Encoding: identity
    payload: '{"sn":"{{sn}}","properties":{"acMode": 1, "inputLimit": 0 }}'

  zendure_x_charge:
    url: http://{{ states('input_text.zendure_setting_ip_address') }}/properties/write
    method: POST
    headers:
      Content-Type: application/json
      Content-Encoding: identity
    payload: '{"sn": "{{sn}}","properties": {"acMode": 1, "inputLimit": {{inputLimit}} }}'

  zendure_x_charge_balancing:
    url: http://{{ states('input_text.zendure_setting_ip_address') }}/properties/write
    method: POST
    headers:
      Content-Type: application/json
      Content-Encoding: identity
    payload: '{"sn":"{{sn}}","properties":{"inputLimit": {{inputLimit}} }}'

  zendure_x_discharge:
    url: http://{{ states('input_text.zendure_setting_ip_address') }}/properties/write
    method: POST
    headers:
      Content-Type: application/json
      Content-Encoding: identity
    payload: '{"sn":"{{sn}}","properties":{"acMode": 2, "outputLimit": {{outputLimit}} }}'

  zendure_x_discharge_balancing:
    url: http://{{ states('input_text.zendure_setting_ip_address') }}/properties/write
    method: POST
    headers:
      Content-Type: application/json
      Content-Encoding: identity
    payload: '{"sn":"{{sn}}","properties":{"outputLimit": {{outputLimit}} }}'

  zendure_quick_discharge:
    url: http://{{ states('input_text.zendure_setting_ip_address') }}/properties/write
    method: POST
    headers:
      Content-Type: application/json
      Content-Encoding: identity
    payload: '{"sn":"{{sn}}","properties":{"acMode": 2, "outputLimit": {{outputLimit}} }}'

  zendure_stop_with_discharging:
    url: http://{{ states('input_text.zendure_setting_ip_address') }}/properties/write
    method: POST
    headers:
      Content-Type: application/json
      Content-Encoding: identity
    payload: '{"sn":"{{sn}}","properties":{"acMode": 2, "outputLimit": 0 }}'

  zendure_stop_with_everything:
    url: http://{{ states('input_text.zendure_setting_ip_address') }}/properties/write
    method: POST
    headers:
      Content-Type: application/json
      Content-Encoding: identity
    payload: '{"sn":"{{sn}}","properties":{"outputLimit": 0, "inputLimit": 0 }}'

  zendure_full_standby:
    url: http://{{ states('input_text.zendure_setting_ip_address') }}/properties/write
    method: POST
    headers:
      Content-Type: application/json
      Content-Encoding: identity
    payload: '{"sn":"{{sn}}","properties":{"smartMode": 0, "outputLimit": 0, "inputLimit": 0 }}'

  zendure_setting:
    url: http://{{ states('input_text.zendure_setting_ip_address') }}/properties/write
    method: POST
    headers:
      Content-Type: application/json
      Content-Encoding: identity
    payload: '{"sn":"{{sn}}","properties":{"{{instellingitem}}": {{instellinginhoud}} }}'
